---
name: quickstart-doc-writer
description: "產出多種類型的技術文件：快速上手指南、API 參考文件、CLI 操作手冊、SDK 整合指南、Changelog、遷移指南。支援多語系輸出（預設繁體中文）。當用戶需要撰寫使用說明、操作手冊、功能教學、API 文件、版本變更記錄時使用。"
---

# 技術文件產生器

根據文件類型產出結構化、高品質的技術文件。

## 文件類型選擇

| 類型 | 適用場景 | 預估閱讀時間 |
|------|----------|-------------|
| **quickstart** (預設) | 新手上手、功能教學、操作手冊 | 5-10 分鐘 |
| **api-reference** | REST/GraphQL API 端點文件 | 按需查閱 |
| **cli-guide** | 命令列工具操作手冊 | 5-15 分鐘 |
| **sdk-guide** | SDK/Library 整合指南 | 10-20 分鐘 |
| **changelog** | 版本變更記錄 | 2-5 分鐘 |
| **migration-guide** | 版本升級/系統遷移指南 | 10-30 分鐘 |

如使用者未指定類型，預設使用 **quickstart**。

## 語言設定

預設輸出繁體中文。使用者可指定其他語言：
- `zh-TW`（預設）：繁體中文
- `zh-CN`：簡體中文
- `en`：English
- `ja`：日本語
- `ko`：한국어
- 或任何其他語言代碼

語言只影響輸出內容，不影響文件結構。技術術語（API name、CLI command、code）保持原文。

---

## Type: quickstart（快速上手指南）

### 三大目標（必達）

1. **第一次就成功**：照做能完成第一個可驗證成果
2. **下一步清楚**：完成後知道接下來做什麼
3. **卡住可自救**：遇錯能用 Troubleshooting 快速排除

## 章節骨架（固定結構）

```markdown
# [產品/功能名稱]

## 這是什麼
<!-- 1-2 句說明這個功能能幫使用者做什麼 -->

## 快速開始
<!-- 最短路徑完成第一個成果，必須有成功判定 -->

## 常見任務
<!-- 2-3 個最常用的 How-to -->

## 卡住了怎麼辦
<!-- 症狀導向的 Troubleshooting -->
```

## 步驟寫法（硬規則）

### 必須遵守

- **一句一動作**：每步只做一件事
- **動作 + 回饋**：告訴讀者「做什麼」+「會看到什麼」
- 用 **粗體** 標示按鈕名稱、選單路徑、重要欄位

### 正確範例

```markdown
1. 點選 **設定 → 帳號**。
2. 在 **API Key** 欄位貼上金鑰。
3. 按 **儲存**，右上角會出現「已更新」提示。
```

### 禁止

- 多個動作塞一句：「打開設定然後找到帳號把金鑰貼進去」
- 模糊詞：「處理一下」「設定好就可以」「按照指示操作」

## 提示框規範

固定使用以下四種：

```markdown
> ✅ **最佳做法**：建議的正確做法

> ⚠️ **注意**：不可逆或高風險操作警告

> 🛠️ **小技巧**：省力捷徑

> 🧯 **卡住了？** 立刻可嘗試的排除方法
```

## 圖片決策規則

依以下條件判斷是否需要圖片：

| 條件 | 需要圖片 | 圖片類型 |
|------|---------|---------|
| UI 導航超過 2 層 | ✅ 必須 | 定位圖 |
| 成功判定不直觀（需看狀態/log） | ✅ 必須 | 成功畫面圖 |
| 高風險/不可逆操作 | ✅ 必須 | 確認對話框圖 |
| 實體操作（接線、按鍵、安裝） | ✅ 必須 | 步驟圖 |
| 常見錯誤點 | 🔶 建議 | 正確 vs 錯誤對照圖 |
| 按鈕位置明顯且單一 | ❌ 不需要 | — |

詳細圖片規格見 `references/image-rules.md`。

## 圖片需求輸出格式

每個需要圖片的位置，輸出以下區塊：

```markdown
<!-- IMAGE_NEEDED
type: [where | success | compare | step]
description: [這張圖要拍/畫什麼]
key_elements: [必須出現的元素，用逗號分隔]
annotation: [需要的標註：框選/箭頭/編號]
alt_text: [替代文字]
caption: [圖說，包含「看哪裡」+「成功判定」]
-->
```

### 範例

```markdown
<!-- IMAGE_NEEDED
type: where
description: 設定頁面的 API Key 輸入區塊
key_elements: 設定選單, 帳號分頁, API Key 欄位, 儲存按鈕
annotation: 框選 API Key 欄位，箭頭指向儲存按鈕
alt_text: 設定頁的帳號區塊，顯示 API Key 輸入欄位與儲存按鈕位置
caption: 在「設定 → 帳號」頁面找到 API Key 欄位，貼上後按「儲存」
-->
```

## 輸出品質檢核

完成文件後自我檢查：

- [ ] 「快速開始」能在 5-10 分鐘內完成
- [ ] 每個步驟都有成功判定
- [ ] UI 導航 > 2 層的地方都有圖片需求
- [ ] 高風險操作有 ⚠️ 警告
- [ ] Troubleshooting 是「症狀 → 解法」格式

---

## Type: api-reference（API 參考文件）

### 章節骨架

```markdown
# [API 名稱] API Reference

## Base URL
<!-- API 基礎路徑 -->

## Authentication
<!-- 認證方式：API Key / OAuth / JWT -->

## Endpoints

### [HTTP Method] [Path]
<!-- 每個 endpoint 一個區塊 -->

#### Request
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|

#### Response
<!-- 成功與錯誤回應範例 -->

## Error Codes
<!-- 全域錯誤代碼表 -->

## Rate Limits
<!-- 限流策略說明 -->
```

### Endpoint 寫法規則

- 每個 endpoint 必須有 **Request 範例** 和 **Response 範例**（含 HTTP status code）
- 用 `curl` 作為主要範例語言，可選附 Python / JavaScript / Go
- 必填參數用 `Required` 標記，可選參數標記預設值
- 錯誤回應至少列出 400、401、404、500

### 範例

```markdown
### POST /api/v1/users

Create a new user account.

**Request:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| email | string | Yes | User email address |
| name | string | Yes | Display name |
| role | string | No | Default: "viewer" |

\`\`\`bash
curl -X POST https://api.example.com/v1/users \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"email": "user@example.com", "name": "Alice"}'
\`\`\`

**Response (201):**

\`\`\`json
{
  "id": "usr_abc123",
  "email": "user@example.com",
  "name": "Alice",
  "role": "viewer",
  "created_at": "2024-01-15T10:30:00Z"
}
\`\`\`

**Error (409):**

\`\`\`json
{
  "error": "conflict",
  "message": "Email already registered"
}
\`\`\`
```

---

## Type: cli-guide（CLI 操作手冊）

### 章節骨架

```markdown
# [CLI 名稱] 使用手冊

## 安裝
<!-- 各平台安裝指令 -->

## 基本用法
<!-- 最常用的 3-5 個指令 -->

## 指令總覽
<!-- 完整指令表格 -->

## 全域選項
<!-- --verbose, --config, --output 等 -->

## 設定檔
<!-- 設定檔路徑、格式、優先級 -->

## 範例
<!-- 真實使用場景的組合指令 -->
```

### 指令文件格式

```markdown
### `command-name`

說明文字。

\`\`\`bash
cli-name command-name [options] <required-arg> [optional-arg]
\`\`\`

| Option | Short | Default | Description |
|--------|-------|---------|-------------|
| --output | -o | stdout | 輸出路徑 |
| --format | -f | json | 輸出格式 (json/yaml/csv) |

**範例：**

\`\`\`bash
# 基本使用
cli-name command-name input.json

# 指定輸出
cli-name command-name -o result.csv -f csv input.json
\`\`\`
```

---

## Type: sdk-guide（SDK 整合指南）

### 章節骨架

```markdown
# [SDK 名稱] 整合指南

## 安裝
<!-- 各套件管理器的安裝指令 -->

## 快速開始
<!-- 最小可運行範例 -->

## 初始化
<!-- Client 初始化、認證設定 -->

## 核心功能
<!-- 主要 API 方法，按功能分組 -->

## 進階用法
<!-- 錯誤處理、重試、分頁 -->

## 型別定義
<!-- TypeScript types / Python dataclass -->

## 遷移指南
<!-- 從舊版升級（如有） -->
```

### 寫法規則

- 每個功能都附 **完整可複製的程式碼範例**
- 範例中使用有意義的變數名稱（不用 `foo`, `bar`）
- 標示 SDK 支援的語言版本（如 Python ≥ 3.8, Node.js ≥ 18）
- 異步/同步 API 都要列出範例

---

## Type: changelog（版本變更記錄）

### 格式規範

遵循 [Keep a Changelog](https://keepachangelog.com/) 格式：

```markdown
# Changelog

## [1.2.0] - 2024-03-15

### Added
- 新增 XXX 功能

### Changed
- 調整 YYY 行為

### Fixed
- 修復 ZZZ 問題 (#issue-number)

### Deprecated
- 即將移除 AAA，請改用 BBB

### Removed
- 移除 CCC

### Security
- 修補 DDD 安全漏洞
```

### 寫法規則

- 每個條目以 **動詞開頭**（新增、修復、調整、移除）
- 關聯 issue/PR 編號
- Breaking changes 加 **⚠️ BREAKING** 前綴
- 按影響程度排序（breaking → added → changed → fixed）

---

## Type: migration-guide（遷移指南）

### 章節骨架

```markdown
# 從 [舊版本] 遷移到 [新版本]

## 總覽
<!-- 為什麼要遷移、主要變更 -->

## 前置條件
<!-- 遷移前必須確認的事項 -->

## Breaking Changes
<!-- 逐一列出不相容變更及修改方式 -->

## 遷移步驟
<!-- 有序的遷移操作 -->

## 驗證
<!-- 遷移後如何確認成功 -->

## Rollback
<!-- 如果出問題如何回滾 -->
```

### 寫法規則

- Breaking change 必須附上 **Before / After 對照程式碼**
- 每一步都要有 **驗證方式**（如何確認這一步成功）
- 提供 rollback 方案
- 列出已知問題和 workaround

### Before / After 範例格式

```markdown
**Before (v1.x):**
\`\`\`javascript
const client = new Client(apiKey);
\`\`\`

**After (v2.x):**
\`\`\`javascript
const client = new Client({ apiKey, version: 'v2' });
\`\`\`
```

---

## 通用品質規則（適用所有文件類型）

### 提示框規範

固定使用以下四種：

```markdown
> ✅ **最佳做法**：建議的正確做法

> ⚠️ **注意**：不可逆或高風險操作警告

> 🛠️ **小技巧**：省力捷徑

> 🧯 **卡住了？** 立刻可嘗試的排除方法
```

### 通用寫法規則

- **一句一動作**：每步只做一件事
- **動作 + 回饋**：告訴讀者「做什麼」+「會看到什麼」
- 用 **粗體** 標示按鈕名稱、選單路徑、重要欄位
- 禁止模糊詞：「處理一下」「設定好就可以」

## 資源

- 圖片詳細規格：`references/image-rules.md`
- 空白模板：`assets/template.md`
- API 文件模板：`assets/api-template.md`

---

## OpenAPI 3.0 Spec 生成指引

當文件類型為 `api-reference` 時，可選擇同時產生 OpenAPI 3.0 規格檔。

### 輸出格式

```yaml
openapi: "3.0.3"
info:
  title: "{API 名稱}"
  version: "{版本號}"
  description: "{API 描述}"

servers:
  - url: "{Base URL}"
    description: "{環境名稱}"

paths:
  /api/v1/{resource}:
    get:
      summary: "{操作摘要}"
      operationId: "{operationId}"
      tags: ["{Tag}"]
      parameters:
        - name: "{參數名}"
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: "{成功描述}"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/{SchemaName}"
        "400":
          description: Bad Request
        "401":
          description: Unauthorized

components:
  schemas:
    "{SchemaName}":
      type: object
      properties:
        id:
          type: string
        name:
          type: string
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
```

### 生成規則

- 每個 API Reference 文件中的 endpoint 都對應一個 `paths` 項目
- Request 參數對應 `parameters`（query/path）或 `requestBody`
- Response 的 JSON 範例轉為 `components/schemas`
- Error codes 對應各 endpoint 的 `responses`
- Authentication 對應 `securitySchemes`

### 使用方式

在產出 API Reference 文件時，追加以下指示即可同時生成 OpenAPI Spec：

```
請同時產出 OpenAPI 3.0 YAML 規格檔
```

---

## 跨文件連結規範

### 場景

當一個專案需要多份文件（如 Quickstart + API Reference + CLI Guide）時，文件間需要互相連結。

### 連結格式

```markdown
<!-- 同層級文件互相連結 -->
詳細 API 說明請參閱 [API Reference](./api-reference.md)。

<!-- 指向特定章節 -->
設定方式請參閱 [CLI Guide - 設定檔](./cli-guide.md#設定檔)。

<!-- 外部連結 -->
完整規格請參閱 [官方文件](https://docs.example.com)。
```

### 連結檢查清單

- [ ] 每份文件的「下一步」段落都有連結到相關文件
- [ ] API Reference 的 Authentication 連結到 Quickstart 的設定段落
- [ ] CLI Guide 的進階選項連結到 API Reference 的對應 endpoint
- [ ] Migration Guide 連結到 Changelog 的 breaking changes
- [ ] 所有內部連結的錨點實際存在
